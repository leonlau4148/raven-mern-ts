MONGO_URI=mongodb://127.0.0.1:27017/ravenmern
JWT_SECRET=replace-with-a-long-random-string
PORT=5000
# Optional. Comma-separated origins allowed to call the API.
# Leave unset locally to allow any origin.
CORS_ORIGINS=
